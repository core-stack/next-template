import { Content } from './content';
import { Header } from './header';
import { Root } from './root';
import { Title } from './title';

export const Page = {
  Root: Root,
  Title: Title,
  Header: Header,
  Content: Content
}